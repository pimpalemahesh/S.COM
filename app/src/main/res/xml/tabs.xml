<?xml version="1.0" encoding="utf-8"?>
<Tabs>
    <tabs>
        <tab
            drawable="@drawable/ic_home"
            text="@string/home" />

        <tab
            drawable="@drawable/ic_notifications"
            text="@string/notification" />

        <tab
            drawable="@drawable/ic_add"
            text="@string/add" />

        <tab
            drawable="@drawable/ic_search"
            text="@string/search" />

        <tab
            drawable="@drawable/ic_user"
            text="@string/profile" />
    </tabs>
</Tabs>